<Window x:Class="NetBootloader.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:NetBootloader.App.ViewModels"
        Title="Net Bootloader" Height="560" Width="720" MinHeight="480" MinWidth="600">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Connection settings -->
        <GroupBox Grid.Row="0" Header="Connection" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Port:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding AvailablePorts}"
                          SelectedItem="{Binding SelectedPort}" VerticalAlignment="Center" />
                <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshPortsCommand}"
                        Margin="6,0,16,0" Padding="8,2" />

                <TextBlock Grid.Column="3" Text="Baud rate:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <ComboBox Grid.Column="4" ItemsSource="{Binding BaudRates}"
                          SelectedItem="{Binding SelectedBaudRate}" IsEditable="True" VerticalAlignment="Center" />

                <TextBlock Grid.Column="5" Text="Timeout (s):" VerticalAlignment="Center" Margin="16,0,6,0" />
                <TextBox Grid.Column="6" Text="{Binding TimeoutSeconds}" VerticalAlignment="Center" />
            </Grid>
        </GroupBox>

        <!-- Firmware file -->
        <GroupBox Grid.Row="1" Header="Firmware" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding HexFilePath, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center" IsReadOnly="True" />
                <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseHexFileCommand}"
                        Margin="6,0,0,0" Padding="8,2" />
            </Grid>
        </GroupBox>

        <!-- Options -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <CheckBox Content="Verify checksum after each write" IsChecked="{Binding VerifyChecksum}"
                      Margin="0,0,20,0" VerticalAlignment="Center" />
            <CheckBox Content="Reset device after flashing" IsChecked="{Binding ResetAfterFlash}"
                      VerticalAlignment="Center" />
        </StackPanel>

        <!-- Actions + progress -->
        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Flash" Command="{Binding FlashCommand}"
                    Padding="16,4" FontWeight="Bold" />
            <Button Grid.Column="1" Content="Cancel" Command="{Binding CancelCommand}"
                    Padding="16,4" Margin="8,0,0,0" />
            <StackPanel Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center">
                <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" Minimum="0" Maximum="100" Height="18" />
                <TextBlock Text="{Binding StatusText}" Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <!-- Log -->
        <GroupBox Grid.Row="4" Header="Log">
            <TextBox Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" TextWrapping="NoWrap" />
        </GroupBox>
    </Grid>
</Window>
